// Kept as a thin alias so older imports keep working; the shared channel now
// lives in liveChannel.ts and also carries finished captions.
export { subscribeLivePartials, subscribeLiveFinals, liveChannelReady } from './liveChannel'
